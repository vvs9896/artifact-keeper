[package]
name = "artifact-keeper-backend"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "artifact-keeper"
path = "src/main.rs"

[features]
vendored-openssl = ["openssl/vendored"]
jemalloc = ["dep:tikv-jemallocator"]
mimalloc = ["dep:mimalloc"]
profiling = ["jemalloc", "dep:tikv-jemalloc-ctl", "dep:tikv-jemalloc-sys", "tikv-jemallocator/profiling"]

[dependencies]
openssl = { workspace = true, optional = true }
# Async runtime
tokio.workspace = true

# Web framework
axum.workspace = true
tower.workspace = true
tower-http.workspace = true

# Database
sqlx.workspace = true

# Serialization
serde.workspace = true
serde_json.workspace = true

# Authentication
jsonwebtoken.workspace = true
bcrypt.workspace = true

# Checksums
sha2.workspace = true
md-5.workspace = true
sha1.workspace = true
hmac.workspace = true
blake2 = "0.10"

# Encryption
aes-gcm.workspace = true
zeroize.workspace = true

# S3 storage using Apache object_store crate
object_store.workspace = true

# HTTP types (presigned URL methods)
http.workspace = true
http-body-util.workspace = true

# TLS (explicit provider selection for rustls 0.23+)
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }

# Logging/Tracing
tracing.workspace = true
tracing-subscriber.workspace = true
tracing-appender = "0.2"

# OpenTelemetry
opentelemetry.workspace = true
opentelemetry_sdk.workspace = true
opentelemetry-http.workspace = true
opentelemetry-otlp.workspace = true
tracing-opentelemetry.workspace = true

# Metrics
metrics.workspace = true
metrics-exporter-prometheus.workspace = true

# Error handling
thiserror.workspace = true
anyhow.workspace = true

# Config
dotenvy.workspace = true

# UUID
uuid.workspace = true

# Time
chrono.workspace = true
cron.workspace = true

# HTTP client
reqwest.workspace = true

# URL encoding
serde_urlencoded.workspace = true

# Async streams
futures.workspace = true
tokio-util.workspace = true
tokio-stream.workspace = true
bytes.workspace = true
# Streaming multipart reader (nuget push: parse the dotnet client's
# multipart/form-data envelope off the body stream without buffering).
multer = "3.1"

# CLI
clap.workspace = true

# Additional
async-trait = "0.1"
async-stream = "0.3"
hex = "0.4"
moka.workspace = true
redis.workspace = true
rand = "0.10"
rand08 = { package = "rand", version = "0.8" }
pgp = "0.14.2"

# Format parsing
quick-xml = { version = "0.41", features = ["serialize"] }
flate2 = "1.0"
tar = "0.4"
xz2 = "0.1"
mime_guess = "2.0"
zip = "8.5"
toml = "1.1"
serde_yaml = "0.9"
regex = "1.11"
once_cell = "1"
bzip2 = "0.6"
zstd = "0.14"
rmp-serde = "1.3"
# Conda semantics reference implementation (#4040): the crates pixi/prefix.dev
# are built on, adopted so registry-side version ordering and MatchSpec
# handling agree with the client exactly. Both are BSD-3-Clause.
# `rattler_package_streaming` keeps default features off: its `reqwest`
# networking module is not needed (we stream from in-memory upload bytes), and
# its `.tar.bz2` reader is single-stream, so the multi-stream v1 path stays on
# our own reader (#4067).
rattler_conda_types = { version = "0.53", default-features = false }
rattler_package_streaming = { version = "0.27", default-features = false }

# WASM runtime
wasmtime.workspace = true
wasmtime-wasi.workspace = true

# Git operations
git2.workspace = true

# Base64 encoding
base64 = "0.22"
totp-rs = { version = "5", features = ["qr", "gen_secret"] }

# Ed25519 signature verification for plugin supply-chain trust.
# Verifies a detached signature over the raw WASM bytes against an
# operator-provisioned trusted public key before a plugin is installed.
ed25519-dalek = "2"

# Search engine
opensearch = { workspace = true }

# Temp files
tempfile = "3.27"

# Memory-mapped scan inputs: lets a multi-GiB artifact be staged to disk and
# handed to scanners as file-backed (kernel-reclaimable) pages instead of anon
# heap. Pairs with `Bytes::from_owner`.
memmap2 = "0.9"

# Directory walking
walkdir = "2.5"
rsa = { version = "0.9", features = ["sha2"] }
urlencoding = "2.1.3"
url.workspace = true
ldap3 = "0.12"
bergshamra = { workspace = true }
native-tls = "0.2"

# OpenAPI
utoipa.workspace = true
utoipa-axum.workspace = true
utoipa-swagger-ui.workspace = true

# gRPC
tonic.workspace = true
tonic-prost.workspace = true
tonic-reflection.workspace = true
prost.workspace = true
prost-types.workspace = true
zxcvbn = "3"

# SMTP email delivery
lettre.workspace = true

# Allocators
mimalloc = { version = "0.1", optional = true }

# Unicode confusable (UTS #39 skeleton) + mixed-script detection for
# typo-squat curation rules (#2956)
unicode-security = "0.1.2"
unicode-normalization = "0.1.25"

# Sigstore attestation verification for `publisher_trust match:attestation`
# (#2955). Pinned exactly — the crate's verify API is documented experimental
# and its internals moved between 0.13 and 0.14, so a caret range would be
# reckless. `default-features = false` keeps the signing/oauth interactive
# surface out; `rustls-tls` selects the ring-compatible TLS stack the backend
# already uses (rustls 0.23 above). `x509-cert` is used to extract the Fulcio
# leaf-certificate identity ourselves AFTER verification (the crate exposes
# only assertion policies, never the parsed identity).
sigstore = { version = "=0.14.0", default-features = false, features = [
    "verify",
    "sigstore-trust-root",
    "rustls-tls",
] }
x509-cert = { version = "0.2", features = ["pem"] }
# RFC 8785 (JCS) canonicalization for the Rekor Signed Entry Timestamp payload
# (#3231). Same crate, same version the `sigstore` dependency above already uses
# internally for exactly this payload in its (private, feature-gated) cosign
# path — matching its encoding byte-for-byte is the whole point.
serde_json_canonicalizer = "0.3"

[target.'cfg(not(windows))'.dependencies]
tikv-jemallocator = { version = "0.6", optional = true }
tikv-jemalloc-ctl = { version = "0.6", optional = true, features = ["stats"] }
tikv-jemalloc-sys = { version = "0.6", optional = true }

[target.'cfg(windows)'.dependencies]
windows-service = "0.8"

[build-dependencies]
tonic-prost-build.workspace = true
prost-build.workspace = true

[dev-dependencies]
tokio-test = "0.4"
axum-test = "19"
wiremock = "0.6"
metrics-util = "0.20"
# Mint self-signed / non-Fulcio leaf certificates for the attestation red-team
# suite (#2955 Stage 4, rows 3 and 8 — the Stage-1 fixtures were missing them).
rcgen = "0.13"
